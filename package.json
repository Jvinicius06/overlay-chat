{
  "name": "overlay-chat",
  "version": "1.0.0",
  "description": "Twitch chat overlay system with multi-channel merge support for IRL streams",
  "type": "module",
  "scripts": {
    "start": "npm run heroku-postbuild && cd server && npm start",
    "heroku-postbuild": "cd server && npm install && cd ../client && npm install && npm run build",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "keywords": [
    "twitch",
    "chat",
    "overlay",
    "sse",
    "websocket",
    "streaming"
  ],
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
